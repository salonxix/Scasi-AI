name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ─── 1. INSTALL (shared cache for all jobs) ─────────────────────
  install:
    name: Install Dependencies
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Cache node_modules
        uses: actions/cache@v5
        with:
          path: node_modules
          key: node-modules-${{ github.sha }}

  # ─── 2. LINT ────────────────────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest
    needs: install
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20

      - name: Restore node_modules cache
        uses: actions/cache@v5
        with:
          path: node_modules
          key: node-modules-${{ github.sha }}

      - name: Run ESLint
        run: npm run lint

  # ─── 3. SECURITY AUDIT ──────────────────────────────────────────
  audit:
    name: Security Audit
    runs-on: ubuntu-latest
    needs: install
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20

      - name: Restore node_modules cache
        uses: actions/cache@v5
        with:
          path: node_modules
          key: node-modules-${{ github.sha }}

      - name: Audit dependencies (high + critical only)
        run: npm audit --audit-level=high

  # ─── 4. TYPE CHECK ──────────────────────────────────────────────
  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    needs: install
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20

      - name: Restore node_modules cache
        uses: actions/cache@v5
        with:
          path: node_modules
          key: node-modules-${{ github.sha }}

      - name: TypeScript type check
        run: npx tsc --noEmit

  # ─── 5. UNIT TESTS + COVERAGE (with threshold enforcement) ──────
  test:
    name: Unit Tests & Coverage
    runs-on: ubuntu-latest
    needs: install
    env:
      NEXTAUTH_SECRET: ci-secret
      NEXTAUTH_URL: http://localhost:3000
      NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
      NEXT_PUBLIC_SUPABASE_ANON_KEY: placeholder-anon-key
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20

      - name: Restore node_modules cache
        uses: actions/cache@v5
        with:
          path: node_modules
          key: node-modules-${{ github.sha }}

      - name: Run tests with coverage
        run: npx jest --ci --runInBand --forceExit --coverage --coverageReporters=text --coverageReporters=lcov

      - name: Upload coverage report
        uses: actions/upload-artifact@v7
        with:
          name: coverage-report
          path: coverage/
          retention-days: 7

  # ─── 6. BUILD (gated on ALL checks passing) ─────────────────────
  build:
    name: Production Build
    runs-on: ubuntu-latest
    needs: [lint, audit, typecheck, test]
    env:
      NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
      NEXTAUTH_URL: ${{ secrets.NEXTAUTH_URL }}
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
      GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
      GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20

      - name: Restore node_modules cache
        uses: actions/cache@v5
        with:
          path: node_modules
          key: node-modules-${{ github.sha }}

      - name: Build
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: nextjs-build-${{ github.sha }}
          path: .next/
          retention-days: 3
